#include<bits/stdc++.h>
using namespace std;

int main(){
	string s;
	cin>>s;
	int n=s.size();
	char ss=0x21;
	cout<<ss;
	for(int i=0;i<94;++i){
		for
	}
	return 0;
}

	for(int i=0;i<n;++i)
    {
        id[i] = i;
        scanf("%d",b + i);
    }
    sort(id,id+n,cmpb);
    for(int i=0;i<n;++i)
    {
        ed[n - i - 1] = id[i];
    }
    
    for(int i=0;i<n;++i)
    {
        ed[i] = i;
        scanf("%d",b + i);
    }
    sort(ed,ed+n,cmpb);
